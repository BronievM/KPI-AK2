=== ПЕРШИЙ ТЕРМІНАЛ ===

# 1 | системна інформація: ядро, ім'я хоста, реліз ядра, версія ядра і дата зборки, архітектура машини, тип процесора
uname -a

# 2 | оновити пакети при появі нових за допомогою менеджера пакетів apt
sudo apt update && sudo apt upgrade

# 3 | вивести вміст файлу, який містить список репозиторіїв
cat /etc/apt/sources.list

# 4 | інсталювати текстовий редактор vim
sudo apt install vim

# 5 | перейти в домашню директорію
cd ~

# 6 | перейти у директорію Downloads
cd ~/Downloads

# 7 | показати мануал для команди ls
man ls

# 8 | вивести весь вміст поточної директорії у long listing format(права доступу, число посилань, ім'я вланика, група власника, розмір, час останньої модифікації, ім'я файлу чи директорії)
ls -la

# 9 | інсталювати пакет за допомогою пакетного менеджера dpkg(виникла помилка через відсутність прав суперюзера)
dpkg -i package.deb

# 10 | та сама команда з правами суперкористувача
sudo dpkg -i package.deb

# 11 | перейти в кореневий каталог
cd /

# 12 | перейти у режим суперкористувача
sudo -i

# 13 | вийти з режиму суперкористувача
exit

# 14 | вивести текст hi all
echo "hi all"

# 15 | вивести значення змінної оточення, яка містить назву поточної директорії та текст hi all here!
echo "$PWD hi all here!"

# 16 | створити файл touch hi.sh у поточній директорії
touch hi.sh

# 17 | вивести значення змінної оточення PATH, яка зберігає всі шляхи до папок, які містять виконувані файли
echo $PATH

# 18 | відкрити файл у редакторі vi
vi hi.sh

# 19 | запуск виконуваного файлу (виникла помика через відсутність прав на виконання)
./hi.sh

# 20 | дати права на виконання файлу усім користувачам
chmod a+x hi.sh

# 21 | видати права на читання, редагування та винання файлу всім користувачам
chmod a+rwx hi.sh

# 22 | запустити файл без параметрів
./hi.sh

# 23 | запустити файл з параметром Ukraine
./hi.sh Ukraine

# 24 | скопіювати файл у папку scripts
mkdir -p scripts && cp hi.sh scripts/

# 25 | скопіювати каталог scripts у каталог Documents (помилка: папка має копіюватися рекурсивно)
cp scripts Documents

# 26 | правильна команда для попередньої дії
cp -r scripts Documents

# 27 | перейти у папку scripts
cd scripts

# 28 | перемістити файл у директорії рівнем вище
mv hi.sh ../

# 29 | перейти у папку на рівень вище
cd ..

# 30 | видалити каталог (помилка: це каталог)
rm scripts

# 31 | правильна команда до минулої дії
rm -rf scripts

# 32 | перейти у папку /bin
cd /bin

# 33 | вивести список файлів поточної директрії у long listing format
ls -l

# 34 | перейти у директорію на два рівні вище
cd ../..

# 35 | створити архів, що містить каталог scripts з детальною інформацією
cd ~ && mkdir -p scripts && tar -cvf scripts.tar scripts/

# 36 | вивести вміст файлу
cat /etc/passwd

# 37 | вивести вміст лог-файлу
cat /var/log/syslog

# 38 | вивести останні 20 рядків лог-файлу
tail -n 20 /var/log/syslog

# 39 | переглянути вміст лог-файлу з можливістю пошуку
less /var/log/syslog

# 40 | перейти в кореневий каталог
cd /

# 41 | знайти у ієрархії поточної директорії файли з назвою dhcp
find . -name "dhcp"

# 42 | знайти у ієрархії поточної директорії файли назва якиx містить dhcp
find . -iname "*dhcp*"

# 43 | знайти у ієрархії поточної директорії файли назва якиx містить dhcp і виділити співпадіння dhcp ігноруючи регістр
grep -i -r "dhcp" /etc

# 44 | знайти у ієрархії поточної директорії файли назва якиx містить dhcp і виділити співпадіння etc ігноруючи регістр
grep -i -r "etc" /etc

# 45 | відправити серію icmp-повідомлень серверу google.com для перевірки з'єднання
ping -c 10 google.com

# 46 | та сама команда тільки у фоновому режимі(одночасно можна виконувати команду date і писати вивід у лог)
(ping google.com &) && date >> ping.log

# 47 | запустити процес відправки icmp-повідомлень з правами суперюзера
sudo ping -c 10 google.com

# 48 | запустити медіаплеєр
vlc &

# 49 | вивести список файлів поточної директрії у long listing format
ls -l

# 50 | зайти в папку /bin
cd /bin

# 51 | вивести вміст поточного каталогу
ls

# 52 | вивести змінну оточення $PATH (містить шляхи до директорій з виконуваними файлами і дозволяє запускати їх не вказуючи повний шлях)
echo "$PATH"

# 53 | вивести всі змінні оболонки
printenv

# 54 | вивести ім'я поточного користувача
whoami

# 55 | перейти в каталог /etc/, у якому знаходяться конфігураційні файли
cd /etc

# 56 | вивести вміст поточного каталогу
ls -l

# 57 | перейти в каталог на рівень вище
cd ..

# 58 | перейти в папку lib, де містяться динамічно зв'язані бібліотеки
cd lib

# 59 | вивести вміст поточного каталогу
ls -l

# 60 | diagnostic messages - вивести повідомлення від ядра(помилка: немає дозволу нв дану операцію)
dmesg

# 61 | та сама команда з правами суперкористувача
sudo dmesg

# 62 | вивести вміст папки /dev(містить точки монтування всіх пристроїв, підключених до системи)
ls -l /dev

# 63 | очистити буфер повідомлень від ядра
sudo dmesg -C

# 64 | підключили флешку, перевіряємо повідомлення від ядра
dmesg | tail -n 20

# 65 | показати всі вмонтовані файлові системи та зайняте місце на диску
df -h

# 66 | перейти в каталог /home
cd /home

# 67 | вивести список файлів поточної директрії у long listing format
ls -l

# 68 | список всіх запущених драверів
lsmod

# 69 | перейти в папку, що містить всі процеси
cd /proc

# 70 | вивести вміст файлу cpuinfo (інформацію про процесор)
cat cpuinfo

# 71 | записати 1 у файл /proc/sys/net/ipv4/ip_forward(помилка: недостатньо прав)
echo 1 > /proc/sys/net/ipv4/ip_forward

# 72 | записати 1 у файл /proc/sys/net/ipv4/ip_forward(помилка: недостатньо прав)
sudo echo 1 > /proc/sys/net/ipv4/ip_forward

# 73 | перейти у режим суперюзера
sudo su

# 74 | переглянути інформацію про конфігурацію мережевого інтерфейсу
ip addr show

# 75 | відобразити інформацію про всі мережеві інтерфейси, навіть непрацюючі
ip link show

# 76 | перейти в домашню директорію
exit 
cd ~

# 77 | записати у файл 1.txt vika
echo "vika" > 1.txt

# 78 | вивести вміст файлу 1.txt
cat 1.txt

# 79 | записати у файл 2.txt vasya
echo "vasya" > 2.txt

# 80 | вивести вміст файлу 2.txt
cat 2.txt

# 81 | порівняти файли 1.txt та 2.txt
diff 1.txt 2.txt

# 82 | вивести історію команд
history

# 83 | вивести інформацію про PCI шини і пристрої, підключені до них
lspci

# 84 | записати історію команд у файл history.txt
whoami; date; history > history.txt


=== ДРУГИЙ ТЕРМІНАЛ ===

# 1 | повернути останній процес у інтерактивний режим
fg

# 2 | вивести інформацію про всі процеси(юзер, який запустив процес; ідентифікатор процесу; час ініціалізації; стан процесу; час виконання процесу; команда, якою було запущено процес), які включають ping
ps aux | grep ping

# 3 | зупинити процес з ідентифікатором 12064(ping google.com) з дозволом зберегти чи очистити дані перед завершенням (сигнал TERM)
kill -15 12064

# 4 | вивести інформацію про всі процеси, команди яких містять ping
ps aux | grep ping

# 5 | зупинити процес 13235(але він продовжує виконуватись, тому що запущений рутом)
kill 13235

# 6 | зупинити процес 13235 примусово з правами користівача(успішно)
sudo kill -9 13235

# 7 | зупинити всі процеси запущені командою vlc з правами суперкористувача
sudo killall vlc

# зберегти історію другого терміналу
whoami; date; history > history_term2.txt

# для скрипта автоматизації 
nano 3.sh
chmod +x 3.sh
./3.sh